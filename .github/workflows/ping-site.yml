name: Ping site every 10 min

on:
  schedule:
    - cron: "3,13,23,33,43,53 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping your site
        run: curl -L --silent --show-error --fail --retry 3 --retry-delay 20 https://form-cjrv.onrender.com/
